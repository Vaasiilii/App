import { NextRequest, NextResponse } from 'next/server';`r`n`r`nexport async function POST(req: NextRequest) {`r`n  const body = await req.json().catch(() => ({} as any));`r`n  if (!body || typeof body.reservationId !== 'string' || !body.reservationId) {`r`n    return NextResponse.json({ error: 'Bad request' }, { status: 400 });`r`n  }`r`n  return NextResponse.json({ status: 'checking' });`r`n}
